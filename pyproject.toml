[project]
name = "human-detect"
version = "0.1.0"
description = "Lightweight single-image multi-person distance and viewing-angle inference."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

